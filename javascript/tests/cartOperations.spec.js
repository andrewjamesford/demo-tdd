const { showCart, addToCart, clearCart } = require("../cartOperations");

describe("Tests for shopping cart operations", () => {
  beforeEach(() => {
   
  });

  test("Describe the test for the shopping cart operateion", () => {
    // Call operation
    // Expect statement
  });
});
